import axios from 'axios';    //axiosを今後使うので読み込んでいます。

const state = {
}

const getters = {
}

const mutations = {
}

const actions = {
}

export default {
    namespaced: true,
    state,
    getters,
    mutations,
    actions
}